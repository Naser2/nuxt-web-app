export default function(context) {
  console.log("IN CHECK-AUTH");

  // if (process.client) {
  //   console.log(
  //     "CONTEXT STATE AUTH =",
  //     context.store.state.auth.actions.verifyAuth()
  //   );
  //   context.store.state.auth.actions.verifyAuth();
  // }
  // context.store.state.dispatch("verifyAuth", context.req);
  // context.store.dispatch("auth/verifyAuth", context.req);
}
